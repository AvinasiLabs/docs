# Lab

This section covers `DeLong Laboratory`, the platform built on the DeLong Protocol.&#x20;

You'll learn about the TEE-secured Jupyter environments that enable privacy-preserving dataset access without exposing raw data, the Python SDK for integrating protocol datasets into research workflows, and the longevity foundation models designed specifically for aging biology and therapeutic discovery research.

The `DeLong Laboratory` provides the following modules:

* Datasets: `delong-datasets` Python SDK for accessing sensitive life sciences datasets with built-in security controls.
* Foundation Models:
* Models SDK:
* Projects:
